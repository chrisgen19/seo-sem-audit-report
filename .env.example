# Database — copy this to .env and fill in values
DATABASE_URL="postgres://myuser:mypassword@localhost:5432/seoaudit"

# NextAuth
AUTH_SECRET="generate-with: openssl rand -hex 32"
AUTH_URL="http://localhost:3000"

# Encryption key for stored API keys (32 bytes hex)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY="your-64-char-hex-string-here"

# Google PageSpeed Insights API key (optional — free, raises rate limit)
# Get one at: https://console.cloud.google.com/apis/credentials
GOOGLE_PSI_API_KEY=""
